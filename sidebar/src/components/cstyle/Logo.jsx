import styled from "styled-components"

const Logo = styled.div `
   text-align: center;
    font-style: normal;
    font-weight: 700;
    font-size: 2rem;
    color: white;

    span{
        color: red;
    }

`;

export default Logo